version: 1
bootstrap:
  command: ./scripts/ptc/bootstrap
  timeout_minutes: 10
verification:
  before_publish: ./scripts/ci/pre-publication
  timeout_minutes: 45
deployment:
  command: ./scripts/ptc/deploy
  timeout_minutes: 20
